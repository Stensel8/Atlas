name: "Pull Request Labeler"

on:
- pull_request_target

jobs:
  filter:
    permissions:
      contents: read
      pull-requests: write
    runs-on: ubuntu-latest
    steps:
    # gh api repos/actions/labeler/commits/[VERSION] --jq '.sha'
    - uses: actions/labeler@bf12e9b00b37c5c0ca2b87b79b2daf7891dbda13 # v7.0.0
