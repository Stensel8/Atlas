---
title: Defaults
description: Run default AtlasFolder scripts
actions:
  # writeStatus strings are truncated by AME Wizard at ~28 characters — keep them short
  - !writeStatus: {status: 'Applying Atlas Folder defaults'}
  - !cmd:
    command: 'cmd /c "reg import .\DEFAULT.reg > nul 2>&1"'
    exeDir: true
    wait: true
    onUpgrade: false
    runas: currentUserElevated
  - !powerShell:
    command: '.\DEFAULT.ps1'
    exeDir: true
    onUpgrade: true
    wait: true
    runas: currentUserElevated
