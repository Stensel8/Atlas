---
title: Remove 'Extract' from Context Menu
description: Removes 'Extract' from Context Menu
actions:
  - !powerShell:
    command: '& (Join-Path $env:windir ''AtlasDesktop\4. Interface Tweaks\Context Menus\Extract\Remove Extract (default).ps1'') -Silent'
    exeDir: true
    wait: true
    runas: currentUserElevated